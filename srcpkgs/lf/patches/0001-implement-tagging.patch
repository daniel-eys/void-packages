From 3d17bc12297a43c59cc824238260e9a67f5df296 Mon Sep 17 00:00:00 2001
From: =?UTF-8?q?Daniel=20Ey=C3=9Fer?= <daniel.eysser@gmail.com>
Date: Sun, 26 Jul 2020 17:41:01 +0200
Subject: [PATCH] implement tagging

---
 complete.go   |  1 +
 doc.go        |  7 ++++-
 docstring.go  |  7 ++++-
 eval.go       |  8 ++++++
 lf.1          |  9 ++++++-
 nav.go        | 71 ++++++++++++++++++++++++++++++++++++++++++++++++++-
 opts.go       |  1 +
 os.go         |  2 ++
 os_windows.go |  2 ++
 ui.go         | 14 +++++-----
 10 files changed, 112 insertions(+), 10 deletions(-)

diff --git complete.go complete.go
index ae5cf68..785091f 100644
--- complete.go
+++ complete.go
@@ -26,6 +26,7 @@ var (
 		"top",
 		"bottom",
 		"toggle",
+		"toggletag",
 		"invert",
 		"unselect",
 		"copy",
diff --git doc.go doc.go
index a15b59f..6cd1083 100644
--- doc.go
+++ doc.go
@@ -27,7 +27,7 @@ The following commands are provided by lf:
     open                     (default 'l' and '<right>')
     top                      (default 'gg' and '<home>')
     bottom                   (default 'G' and '<end>')
-    toggle
+    toggletag                (default 't')
     invert                   (default 'v')
     unselect                 (default 'u')
     glob-select
@@ -182,6 +182,11 @@ Marks file should be located at:
     unix     ~/.local/share/lf/marks
     windows  C:\Users\<user>\AppData\Local\lf\marks
 
+Tags file should be located at:
+
+    unix     ~/.local/share/lf/tags
+    windows  C:\Users\<user>\AppData\Local\lf\tags
+
 History file should be located at:
 
     unix     ~/.local/share/lf/history
diff --git docstring.go docstring.go
index 87392fd..26515ac 100644
--- docstring.go
+++ docstring.go
@@ -30,7 +30,7 @@ The following commands are provided by lf:
     open                     (default 'l' and '<right>')
     top                      (default 'gg' and '<home>')
     bottom                   (default 'G' and '<end>')
-    toggle
+    toggletag                (default 't')
     invert                   (default 'v')
     unselect                 (default 'u')
     glob-select
@@ -186,6 +186,11 @@ Marks file should be located at:
     unix     ~/.local/share/lf/marks
     windows  C:\Users\<user>\AppData\Local\lf\marks
 
+Tags file should be located at:
+
+    unix     ~/.local/share/lf/tags
+    windows  C:\Users\<user>\AppData\Local\lf\tags
+
 History file should be located at:
 
     unix     ~/.local/share/lf/history
diff --git eval.go eval.go
index ec82326..1ab0757 100644
--- eval.go
+++ eval.go
@@ -698,6 +698,14 @@ func (e *callExpr) eval(app *app, args []string) {
 		app.nav.bottom()
 		app.ui.loadFile(app.nav)
 		app.ui.loadFileInfo(app.nav)
+	case "toggletag":
+		app.nav.toggleTag()
+		if err := app.nav.writeTags(); err != nil {
+			app.ui.echoerrf("tag: %s", err)
+		}
+		if err := remote("send sync"); err != nil {
+			app.ui.echoerrf("tag: %s", err)
+		}
 	case "toggle":
 		if len(e.args) == 0 {
 			app.nav.toggle()
diff --git lf.1 lf.1
index 62554f5..277a4c6 100644
--- lf.1
+++ lf.1
@@ -37,7 +37,7 @@ The following commands are provided by lf:
     open                     (default 'l' and '<right>')
     top                      (default 'gg' and '<home>')
     bottom                   (default 'G' and '<end>')
-    toggle
+    toggletag                (default 't')
     invert                   (default 'v')
     unselect                 (default 'u')
     glob-select
@@ -205,6 +205,13 @@ Marks file should be located at:
     windows  C:\eUsers\e<user>\eAppData\eLocal\elf\emarks
 .EE
 .PP
+Tags file should be located at:
+.PP
+.EX
+    unix     ~/.local/share/lf/tags
+    windows  C:\eUsers\e<user>\eAppData\eLocal\elf\etags
+.EE
+.PP
 History file should be located at:
 .PP
 .EX
diff --git nav.go nav.go
index fdf1d00..8ab44f3 100644
--- nav.go
+++ nav.go
@@ -272,6 +272,7 @@ type nav struct {
 	dirCache        map[string]*dir
 	regCache        map[string]*reg
 	saves           map[string]bool
+	tags            map[string]bool
 	marks           map[string]string
 	renameOldPath   string
 	renameNewPath   string
@@ -365,6 +366,7 @@ func newNav(height int) *nav {
 		dirCache:        make(map[string]*dir),
 		regCache:        make(map[string]*reg),
 		saves:           make(map[string]bool),
+		tags:            make(map[string]bool),
 		marks:           make(map[string]string),
 		selections:      make(map[string]int),
 		selectionInd:    0,
@@ -609,6 +611,21 @@ func (nav *nav) toggleSelection(path string) {
 	}
 }
 
+func (nav *nav) toggleTag() {
+	curr, err := nav.currFile()
+	if err != nil {
+		return
+	}
+
+	if nav.tags[curr.path] {
+		delete(nav.tags, curr.path)
+	} else {
+		nav.tags[curr.path] = true
+	}
+
+	nav.down(1)
+}
+
 func (nav *nav) toggle() {
 	curr, err := nav.currFile()
 	if err != nil {
@@ -897,7 +914,15 @@ func (nav *nav) sync() error {
 		nav.saves[f] = cp
 	}
 
-	return nav.readMarks()
+	if err := nav.readMarks(); err != nil {
+		return err
+	}
+
+	if err := nav.readTags(); err != nil {
+		return err
+	}
+
+	return nil
 }
 
 func (nav *nav) cd(wd string) error {
@@ -1161,6 +1186,29 @@ func (nav *nav) readMarks() error {
 	return nil
 }
 
+func (nav *nav) readTags() error {
+	nav.tags = make(map[string]bool)
+	f, err := os.Open(gTagsPath)
+	if os.IsNotExist(err) {
+		return nil
+	}
+	if err != nil {
+		return fmt.Errorf("opening tags file: %s", err)
+	}
+	defer f.Close()
+
+	scanner := bufio.NewScanner(f)
+	for scanner.Scan() {
+		nav.tags[scanner.Text()] = true
+	}
+
+	if err := scanner.Err(); err != nil {
+		return fmt.Errorf("reading tags file: %s", err)
+	}
+
+	return nil
+}
+
 func (nav *nav) writeMarks() error {
 	if err := os.MkdirAll(filepath.Dir(gMarksPath), os.ModePerm); err != nil {
 		return fmt.Errorf("creating data directory: %s", err)
@@ -1188,6 +1236,27 @@ func (nav *nav) writeMarks() error {
 	return nil
 }
 
+func (nav *nav) writeTags() error {
+	if err := os.MkdirAll(filepath.Dir(gTagsPath), os.ModePerm); err != nil {
+		return fmt.Errorf("creating data directory: %s", err)
+	}
+
+	f, err := os.Create(gTagsPath)
+	if err != nil {
+		return fmt.Errorf("creating tags file: %s", err)
+	}
+	defer f.Close()
+
+	for k := range nav.tags {
+		_, err = f.WriteString(fmt.Sprintf("%s\n", k))
+		if err != nil {
+			return fmt.Errorf("writing tags file: %s", err)
+		}
+	}
+
+	return nil
+}
+
 func (nav *nav) currDir() *dir {
 	return nav.dirs[len(nav.dirs)-1]
 }
diff --git opts.go opts.go
index 6e24f28..e5c7e7b 100644
--- opts.go
+++ opts.go
@@ -121,6 +121,7 @@ func init() {
 	gOpts.keys["G"] = &callExpr{"bottom", nil, 1}
 	gOpts.keys["<end>"] = &callExpr{"bottom", nil, 1}
 	gOpts.keys["<space>"] = &listExpr{[]expr{&callExpr{"toggle", nil, 1}, &callExpr{"down", nil, 1}}, 1}
+	gOpts.keys["t"] = &callExpr{"toggletag", nil, 1}
 	gOpts.keys["v"] = &callExpr{"invert", nil, 1}
 	gOpts.keys["u"] = &callExpr{"unselect", nil, 1}
 	gOpts.keys["y"] = &callExpr{"copy", nil, 1}
diff --git os.go os.go
index 1476a06..5036496 100644
--- os.go
+++ os.go
@@ -32,6 +32,7 @@ var (
 	gUser        *user.User
 	gConfigPaths []string
 	gMarksPath   string
+	gTagsPath    string
 	gHistoryPath string
 )
 
@@ -88,6 +89,7 @@ func init() {
 	}
 
 	gMarksPath = filepath.Join(data, "lf", "marks")
+	gTagsPath = filepath.Join(data, "lf", "tags")
 	gHistoryPath = filepath.Join(data, "lf", "history")
 
 	gDefaultSocketPath = filepath.Join(os.TempDir(), fmt.Sprintf("lf.%s.sock", gUser.Username))
diff --git os_windows.go os_windows.go
index 538278f..505a86e 100644
--- os_windows.go
+++ os_windows.go
@@ -31,6 +31,7 @@ var (
 	gUser        *user.User
 	gConfigPaths []string
 	gMarksPath   string
+	gTagsPath    string
 	gHistoryPath string
 )
 
@@ -72,6 +73,7 @@ func init() {
 	}
 
 	gMarksPath = filepath.Join(data, "lf", "marks")
+	gMarksPath = filepath.Join(data, "lf", "tags")
 	gHistoryPath = filepath.Join(data, "lf", "history")
 }
 
diff --git ui.go ui.go
index 94232a9..eee1a30 100644
--- ui.go
+++ ui.go
@@ -313,7 +313,7 @@ func fileInfo(f *file, d *dir) string {
 	return info
 }
 
-func (win *win) printDir(screen tcell.Screen, dir *dir, selections map[string]int, saves map[string]bool, colors styleMap, icons iconMap) {
+func (win *win) printDir(screen tcell.Screen, dir *dir, selections map[string]int, saves map[string]bool, tags map[string]bool, colors styleMap, icons iconMap) {
 	if win.w < 5 || dir == nil {
 		return
 	}
@@ -381,13 +381,15 @@ func (win *win) printDir(screen tcell.Screen, dir *dir, selections map[string]in
 		path := filepath.Join(dir.path, f.Name())
 
 		if _, ok := selections[path]; ok {
-			win.print(screen, lnwidth, i, st.Background(tcell.ColorPurple), " ")
+			win.print(screen, lnwidth, i, st.Foreground(tcell.ColorPurple), "●")
 		} else if cp, ok := saves[path]; ok {
 			if cp {
-				win.print(screen, lnwidth, i, st.Background(tcell.ColorOlive), " ")
+				win.print(screen, lnwidth, i, st.Foreground(tcell.ColorOlive), "●")
 			} else {
-				win.print(screen, lnwidth, i, st.Background(tcell.ColorMaroon), " ")
+				win.print(screen, lnwidth, i, st.Foreground(tcell.ColorMaroon), "●")
 			}
+		} else if tags[path] {
+			win.print(screen, lnwidth, i, st.Foreground(tcell.ColorDarkCyan), "●")
 		}
 
 		if i == dir.pos {
@@ -763,7 +765,7 @@ func (ui *ui) draw(nav *nav) {
 
 	doff := len(nav.dirs) - length
 	for i := 0; i < length; i++ {
-		ui.wins[woff+i].printDir(ui.screen, nav.dirs[doff+i], nav.selections, nav.saves, ui.styles, ui.icons)
+		ui.wins[woff+i].printDir(ui.screen, nav.dirs[doff+i], nav.selections, nav.saves, nav.tags, ui.styles, ui.icons)
 	}
 
 	switch ui.cmdPrefix {
@@ -789,7 +791,7 @@ func (ui *ui) draw(nav *nav) {
 			preview := ui.wins[len(ui.wins)-1]
 
 			if curr.IsDir() {
-				preview.printDir(ui.screen, ui.dirPrev, nav.selections, nav.saves, ui.styles, ui.icons)
+				preview.printDir(ui.screen, ui.dirPrev, nav.selections, nav.saves, nav.tags, ui.styles, ui.icons)
 			} else if curr.Mode().IsRegular() {
 				preview.printReg(ui.screen, ui.regPrev)
 			}
-- 
2.28.0

